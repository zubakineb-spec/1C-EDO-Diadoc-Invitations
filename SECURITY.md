# Security

This project must not contain Diadoc access tokens, API keys, passwords, certificates, private keys, or other credentials.

The adapter is designed to reuse authentication from the installed Kontur.Diadoc integration module. If credentials are ever required for diagnostics, keep them outside Git and outside the EPF source tree.
